{
  "name": "learn-api",
  "version": "1.0.0",
  "description": "demo how to use json-server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run gen-data && node index.js",
    "dev": "nodemon index.js",
    "gen-data": "node generate-data.js"
  },
  "keywords": [
    "json-server"
  ],
  "author": "Toan Le",
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^2.0.15"
  },
  "dependencies": {
    "faker": "^5.5.3",
    "json-server": "^0.17.0",
    "query-string": "^7.0.1"
  }
}
